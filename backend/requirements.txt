fastapi==0.141.1
uvicorn[standard]==0.52.4
pdfplumber==0.11.10
reportlab==5.0.1
python-multipart==0.0.32
pydantic==2.13.5
pytesseract==0.3.13
# pytesseract is the Python side of OCR only. It calls out to the system
# `tesseract` binary, which isn't installable via pip - it needs an apt
# package (tesseract-ocr) in the deploy environment. This native Python
# Render service has no apt access, so OCR will report "unavailable" here
# until the app is deployed as a Docker service with that binary installed.
